version: '3'
services:
    postgres:
        image: 'postgres:latest'
        environment: 
            - POSTGRES_PASSWORD=postgres_password
    redis:
        image: 'redis:latest'
    express-server:
        build:
            dockerfile: Dockerfile.dev
            context: ./express-server
        volumes:
            - /factorial-project/node_modules
            - ./express-server:/factorial-project
        environment: 
            - REDIS_HOST=redis
            - REDIS_PORT=6379 # Check port from dockerhub
            - PGUSER=postgres
            - PGHOST=postgres
            - PGDATABASE=postgres
            - PGPASSWORD=postgres_password
            - PGPORT=5432
        depends_on:
            - postgres
    react-client:
        build:
            dockerfile: Dockerfile.dev
            context: ./react-client
        volumes:
            - /factorial-project/node_modules
            - ./react-client:/factorial-project
    worker:
        build:
            dockerfile: Dockerfile.dev
            context: ./worker
        volumes:
            - /factorial-project/node_modules
            - ./worker:/factorial-project
        environment:
            - REDIS_HOST=redis
            - REDIS_PORT=6379
    nginx:
        restart: always
        build:
            dockerfile: Dockerfile.dev
            context: ./nginx
        ports:
            - '8000:80'
        depends_on:
            - react-client
            - express-server
